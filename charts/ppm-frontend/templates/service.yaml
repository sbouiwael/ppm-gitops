apiVersion: v1
kind: Service
metadata:
  name: {{ include "ppm-frontend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ppm-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ppm-frontend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP