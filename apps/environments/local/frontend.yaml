apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ppm-frontend-local
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/sbouiwael/ppm-gitops.git
    targetRevision: main
    path: charts/ppm-frontend
    helm:
      valueFiles:
        - values.yaml
        - values-local.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: ppm-local
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
