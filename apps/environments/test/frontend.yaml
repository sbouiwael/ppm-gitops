apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ppm-frontend-test
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/sbouiwael/ppm-gitops.git
    targetRevision: main
    path: charts/ppm-frontend
    helm:
      valueFiles:
        - values.yaml
        - values-test.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: ppm-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
